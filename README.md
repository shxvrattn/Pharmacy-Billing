# Pharmacy-Billing
This is a basic python program which has two files main.py and med.csv. med.csv is the csv file which stores medicines as ["name", quantity, price] in it. It adds to the stock, deletes the stock, bills using the given stock and updates the stock.
